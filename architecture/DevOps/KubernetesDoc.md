What is a Pod, and why not deploy containers directly?
What's the difference between a Deployment, ReplicaSet, and StatefulSet?
What is a Service, and what's the difference between ClusterIP, NodePort, and LoadBalancer?
What is an Ingress, and how is it different from a Service?
What's the difference between a ConfigMap and a Secret?
How does Kubernetes handle rolling updates and rollbacks?
What are liveness probes vs readiness probes?
What is a Namespace and why use one?
How does horizontal pod autoscaling (HPA) work?
What happens when a Pod crashes — how does Kubernetes recover it?
What's the difference between kubectl apply and kubectl create?
How do resource requests and limits (CPU/memory) work, and why do they matter?